version: 0.1
storage:
    cache:
        blobdescriptor: inmemory
    filesystem:
        rootdirectory: {{.RegistryDir}}
auth:
  htpasswd:
    realm: basic-realm
    path: {{.HtpasswdPath}}
http:
    addr: :5000
    secret: {{.Secret}}
